/* This program tells zise of operators
 written by Zahidul Amin */
#include <stdio.h>
#include <stdlib.h>

int main()
{

    printf("Hello world!\n the size  of char %u", sizeof(char));
    printf("Hello world!\n the size of short %u", sizeof(short));
    printf("Hello world!\n the size of int %u", sizeof(int));
    printf("Hello world!\n the size of long long %u", sizeof(long long));
    printf("Hello world!\n the size of float %u", sizeof(float));
    printf("Hello world!\n the size of double %u", sizeof(double));
    printf("Hello world!\n the size of long double %u", sizeof(long double));
    return 0;
}
